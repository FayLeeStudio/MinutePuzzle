# 项目说明（CLAUDE.md）— MinutePuzzle

> 24 小时 GameJam 项目，单人开发，主题：**1min（一分钟）**。
> 开发以 vibe coding 为主。请优先保证“随时有一个能跑的版本”，而不是堆功能。

---

## 一、这是什么

一个**拼图游戏**，但被拼的“图”不是静态画，而是一块**数字计时器显示屏**，
数字从 **59 实时倒数到 00**。玩家要在画面不断变化的过程中，把拼图拼完整。

**核心立意**：你拼的那张图，本身就是计时器——拼图内容 = 剩余时间，二者是同一个东西。

## 二、技术约束（重要，别违背）

- **单文件优先**：尽量都写在一个 `index.html` 里（HTML + CSS + JS 同文件）。太大了再议，拆分前先说一声。
- **纯原生**：vanilla JavaScript + Canvas 2D。**不引入任何框架/库/npm/构建步骤**，除非我明确同意。
- **零构建**：双击 `index.html` 浏览器就能跑。
- 用到图片/音频等本地资源时，提醒我可能要起本地服务器（`python -m http.server`）。
- 音频：**首帧不要自动播声音**，等用户首次交互后再启动。

## 三、核心玩法

- 中间是一个 **3×4 = 12 格**的拼合区（网格）。
- 被拼的图是一块**实时倒计时显示屏**：数字从 **59 → 00**，每秒一跳。
- 12 块**方形碎片**初始散在**外侧的放置区**，玩家**拖拽**碎片到中间网格对应格子。
- 全部碎片放到正确位置 = 拼图完成。

### 设计灵魂（理解错就全废，务必照此实现）

- **碎片不是固定图片，是“开了窗的取景框”**：每块碎片显示的是那块实时显示屏在**对应区域、当前这一刻**的画面。时钟一跳，所有碎片内容同步变化。
- 只有当所有碎片都在**正确格子且对齐**时，中间才拼成一个能正常读的倒计时；拼错就是错位的数字残片。
- **不要给碎片烤静态快照**，那样就失去了“在变化中拼”的核心体验。

### 评分判定（已确定）

- 倒计时归零（到 **00**）= 时间到。**到 00 仍未拼完 → 判负。**
- 在归零前把 12 块全部拼对 → **胜利**，可记录“剩余时间 / 完成时的数字”作为成绩。
- 结束后可一键重玩。

### 这个机制的核心难点（明天专门琢磨，别让它默认做歪）

- 数字每秒在变，玩家**无法靠记忆图案**拼（这块的内容下一秒就变），只能靠**空间位置/碎片在网格里的相对关系**判断——这是机制最妙处，也是难点。
- 风险：某些碎片（纯背景、相似段）可能难以区分甚至让人挫败。**当前决定：先不加任何辅助提示**（不加格子编号、不加正确位置高亮、不加碎片提示），就用最纯粹的“靠空间位置判断”来玩。先验这个纯版本的手感，要不要补提示等玩过再说。

## 四、关键实现注意

- **倒计时用时间戳**：记 `startTime = performance.now()`，每帧算 `elapsed`，由此得出当前应显示的数字（`59 - floor(elapsed)`），到 0 结束。别逐帧累加。
- **显示屏渲染（已定：七段数码管风格）**：把整块倒计时显示屏每帧画到一个**离屏 canvas**（offscreen）上，用**七段数码管**呈现两位数字——每个数字由 7 个段（横竖矩形/梯形）组成，按当前数字点亮对应段（建立 0–9 各数字点亮哪几段的映射表）。点亮段用亮色、熄灭段用极暗底色，营造数码管质感。
- **碎片 = 源画面的子矩形**：每块碎片用 `ctx.drawImage(srcCanvas, sx, sy, sw, sh, dx, dy, dw, dh)` 把显示屏对应子区域，画到碎片当前所在位置。碎片放到正确格子时其子矩形正好对齐 → 整体读得通。这是干净的标准做法。
- **拖拽**：用 pointer 事件（`pointerdown`/`pointermove`/`pointerup`，自动覆盖鼠标+触屏）。拖动中跟随指针，松手时**吸附到最近的合法格子**；判断该格是否为该碎片的正确位置。
- 注意 pointer 在窗口外松开的兜底（`pointercancel`/失焦），避免碎片“粘”在手上。

### 后续扩展（先把上面基础做扎实，再逐个加；加前跟我确认）

- 提高难度：取消位置提示、打乱更碎（4×5 等）、加入“变化中的干扰”。
- 其他主题变体留到核心稳定后再谈。

## 五、代码结构约定

按这五块组织，清晰、加注释，方便我跟得上：

1. 画布与画笔（主 canvas + 离屏 canvas）
2. 游戏状态（集中放：startTime、各碎片的当前位置/目标格子/是否已就位、是否胜负等）
3. 输入（拖拽：pointer 事件）
4. 游戏循环（requestAnimationFrame：每帧 update（推进倒计时、判胜负）→ draw（重画显示屏、画所有碎片））
5. 启动

## 六、给你的工作方式（agent 行为）

- **小步快跑**：一次只改一件事，改完说清改了什么、我该怎么验证。
- 较大改动先用 **plan 模式**给我看方案，确认再动。
- **每到一个能跑的稳定版本，提醒我**（我会用 checkpoint 留档/回退）。
- **保持简单**，能简单实现就别过度设计。不擅自加依赖、扩 scope，拿不准就问。
- 代码可读、有注释——我要看得懂自己的工程。

## 七、优先级（时间压力下的取舍）

1. 永远有一个**能跑、能玩完一局**的版本最重要。
2. 先做到“能拖、能拼对、倒计时会动、到点判胜负”，再谈美化/难度/干扰。
3. 临近结束**停止加功能**，只修 bug、调平衡、确保能提交。

## 八、提交收尾清单（jam 最后 1~2 小时照这个走）

> 本次 jam 提交要求：**itch.io 可玩链接 + GitHub 源码链接，两个都要。**

- [ ] 干净浏览器里完整玩一局，确认能拖、能拼、倒计时正常、胜负判定对。
- [ ] **itch.io**：`index.html`(+资源) 打包 zip，**index.html 在 zip 根目录**（别套文件夹）；勾「This file will be played in the browser」；设好视口尺寸；亲自打开页面玩一遍确认能加载。
- [ ] **GitHub**：推送源码；仓库设 **public**（或评审可访问）；README 写清玩法、操作、素材出处。
- [ ] jam 提交页同时填 **itch 链接 + GitHub 链接**。
- [ ] **提前交一版能跑的**，别卡最后一秒。

## 九、待我确认的事项

- ~~胜负/计时规则~~ → **已定：到 00 未拼完判负；归零前拼完胜利。**
- ~~辅助提示~~ → **已定：先不加任何提示**，做最纯粹的版本。
- ~~显示屏风格~~ → **已定：七段数码管。**
