# 项目说明（CLAUDE.md）— MinutePuzzle

> 单人开发，主题：**1min（一分钟）**。
> 开发以 vibe coding 为主。请优先保证”随时有一个能跑的版本”，而不是堆功能。

---

## 一、这是什么

一个**多关卡拼图游戏**。每关都是一局「**1 分钟拼图**」：中间有一张**活的图**（图案在实时变化），玩家要趁倒计时归零前，把打散的碎片拖回正确位置、拼成完整画面。

**核心立意**：1 分钟倒计时是所有关卡的灵魂——**时间压力 + 动态画面**，两者缺一不可。

- 关卡之间**画面内容不同**（数字屏 / 程序动画 / 视频 / 摄像头），**机制也可以有小变化**（吸附方式、判定、干扰等）。
- 但**「1 分钟限制 + 拖拽拼图 + 在变化中拼」这套核心永远不变**。

**当前关卡清单（线性闯关，5 关，统一 4×3=12 块）：**

| # | 名称 | 内容 | 渲染 |
|---|------|------|------|
| 1 | 倒计时 | 七段数码管 60→00 | `drawSevenSeg`（详见四节，写新关的范例） |
| 2 | 贪吃蛇 | 自动贪吃蛇沿哈密顿回路、60s 渐填满 | `drawSnake` |
| 3 | 影像一 | 视频 `VideoAsset1.mp4` | `drawVideoSrc` |
| 4 | 影像二 | 视频 `VideoAsset0.mp4` | `drawVideoSrc` |
| 5 | 摄像头 | 实时摄像头（`usesCamera` 标记） | `drawCamera` |

**关卡推进方式：线性闯关**——过一关 → 结算 → 「下一关」，依次解锁。先不做自由选关界面（以后要再加）。

---

## 二、通用框架（永远成立，所有关卡共享）

这一节是**引擎层**的约定，写新关卡时不要破坏：

- **时长**：每关默认 **60 秒**倒计时（`duration`，保留可调但一般不动）。
- **核心循环**：选关/进入 → 看一眼完整画面 → 点开始，碎片瞬间打散到外围放置区 → 倒计时开始 → 玩家拖拽碎片回网格 → 胜/负 → 重玩或下一关。
- **碎片 = 实时取景框**：碎片显示的是「被拼的图」在**对应区域、当前这一刻**的画面，不是静态快照。画面一变，所有碎片内容同步变化。**绝不要给碎片烤静态快照**——那样就丢了「在变化中拼」的灵魂。
- **拖拽 + 吸附**：pointer 事件拖动，松手吸附到最近的合法空格子；放对位置不锁定，可随时再拖。
- **胜负判定（默认）**：所有碎片都在各自正确格子 = 胜利，定格此刻剩余秒数作成绩；倒计时越过 00 仍未拼完 = 判负。**关卡可用钩子覆盖这套默认判定**（见三节 `checkWin`）。
- **结束后**可一键重玩；胜利后可进下一关。

> 引擎只认「通用框架 + 关卡配置」。**新关卡 = 写一个新的配置对象，而不是改引擎主流程。** 要改引擎本身先说一声。

---

## 三、关卡架构（配置对象 + 可选钩子）

关卡列表统一放在一处（如 `index.html` 顶部的 `LEVELS` 数组），方便增删。每个关卡是一个**配置对象**：

### 必填字段

- `id`：关卡编号。
- `name`：关卡名。
- `grid`：网格尺寸，如 `{ cols: 4, rows: 3 }`（= 碎片块数）。
- `renderFrame(octx, off, ctx)`：**每关唯一必须定制的部分**。每帧把「被拼的图」画到离屏 canvas（`off` / `octx`）上。引擎再用 `drawImage` 从这块离屏画面给每块碎片取对应子矩形。`ctx` 里带当前帧信息（如 `elapsed`、`remain` 剩余秒数、`cell` 格子尺寸等，按需取用）。

### 可选字段

- `duration`：倒计时秒数，默认 60。
- `usesCamera: true`：标记该关用摄像头。`loadLevel` 据此「进则申请、离则关闭」摄像头，**与关卡顺序/编号解耦**（重排关卡不会错位）。视频关无需标记，`drawVideoSrc` 自己懒加载/预加载。
- 其他开关式配置（按需添加，能不加就不加）：是否显示提示、吸附距离系数等。

### 可选生命周期钩子（机制微调就靠它）

**不写钩子就走通用框架的默认行为；写了就能在那个时机插入这一关特有的小变化。** 钩子拿到引擎的 `state`，尽量只读/小改，别在钩子里重写整套流程。

- `onStart(state)`：碎片打散、倒计时开始后调用一次。适合做这关的初始化（如额外干扰物、特殊散布）。
- `onDrop(state, piece, cell)`：一块碎片吸附到某格后调用。适合做「放置时的机制变化」。
- `checkWin(state)`：覆盖默认胜利判定，返回 `true` 即胜。不写则用「全部碎片归位」的默认判定。
- （需要别的时机再加新钩子，但**加钩子前先跟我确认**，避免引擎接口越长越乱。）

> 设计原则：**能用配置字段表达的就别上钩子；能用一个钩子的就别拆成多个。** 关卡之间是「同一套引擎的不同配置」，不是「各写各的小游戏」。

---

## 四、第一关定义（数码管倒计时屏）—— 也是「怎么写一关」的范例

> 这关已完成，既是正式第一关，也当作后续写新关卡的参照样板。

### 内容

- 网格 **4 列 × 3 行 = 12 格**（贴合两位数字的宽屏比例）。
- 被拼的图 = 一块**实时七段数码管显示屏**，数字 **60 → 00**，每秒一跳。`renderFrame` 据剩余秒数点亮对应段。
- 12 块方形碎片初始拼好摆在正确格子（让玩家先看清完整的钟），点开始后打散到外围。

### 这一关的灵魂（理解错就全废）

- 数字每秒在变，玩家**无法靠记忆图案**拼（内容下一秒就变），只能靠**空间位置 / 碎片在网格里的相对关系**判断——这是机制最妙处，也是难点。
- 数码管骨架**对齐网格中线**：每块碎片的数字内容都落在自己的中线上，所以单看一块判断不出该放哪，逼玩家用空间关系拼。
- 只有所有碎片都在**正确格子且对齐**时，中间才拼成能正常读的倒计时；拼错就是错位的数字残片。

### 这一关的判定

- 用通用框架的默认判定（全部归位 = 胜；越过 00 未拼完 = 负），未用 `checkWin` 钩子。

### 难点 & 当前取舍

- 某些碎片（纯背景、相似段）可能难区分甚至让人挫败。**当前决定：先不加任何辅助提示**（不加格子编号、不加正确位置高亮、不加碎片提示），用最纯粹的「靠空间位置判断」来玩。先验纯版本手感，要不要补提示玩过再说。

---

## 五、技术约束（重要，别违背）

- **单文件优先**：尽量都写在一个 `index.html` 里（HTML + CSS + JS 同文件）。太大了再议，拆分前先说一声。
- **纯原生**：vanilla JavaScript + Canvas 2D。**不引入任何框架/库/npm/构建步骤**，除非我明确同意。
- **零构建**：纯前端、无打包步骤。
  - 倒计时 / 贪吃蛇关：双击 `index.html` 即可跑。
  - **视频关**：浏览器禁 `file://` 加载本地视频，**必须**走本地服务器（`python3 -m http.server`）或线上托管。
  - **摄像头关**：需**安全环境**（`https` 或 `localhost`）+ 用户授权。
- **外部素材现状**：已引入视频素材 `VideoAsset0.mp4` / `VideoAsset1.mp4`（提交时 README 要写出处/授权）。数码管、贪吃蛇仍是纯代码绘制。
- 音频：**首帧不要自动播声音**，等用户首次交互后再启动（视频统一**静音**循环播放，规避自动播放限制）。

---

## 六、关键实现注意

- **倒计时用时间戳**：记 `startTime = performance.now()`，每帧算 `elapsed`，由此得出剩余秒数（`duration - floor(elapsed)`），到 0 结束。别逐帧累加。
- **显示屏渲染走离屏 canvas**：每关的 `renderFrame` 把整块「被拼的图」每帧画到一个**离屏 canvas**（尺寸 = 网格尺寸），碎片再从它取子矩形。
  - 第一关用**七段数码管**呈现两位数字：每个数字由 7 个段组成，按当前数字点亮对应段（`SEGMAP` 建立 0–9 各点亮哪几段的映射表）。点亮段亮色 + 辉光，熄灭段不画。
- **碎片 = 源画面的子矩形**：用 `ctx.drawImage(off, sx, sy, sw, sh, dx, dy, dw, dh)` 把离屏对应子区域画到碎片当前位置。碎片放对格子时其子矩形正好对齐 → 整体读得通。
- **拖拽**：用 pointer 事件（`pointerdown`/`pointermove`/`pointerup`，自动覆盖鼠标 + 触屏）。拖动跟随指针，松手吸附到最近合法空格子；判断该格是否为该碎片正确位置。
- 注意 pointer 在窗口外松开的兜底（`pointercancel`/失焦），避免碎片「粘」在手上。
- **自适应布局**：画布尺寸、格子大小、网格位置随窗口实时计算（`resize()`），缓冲区与显示尺寸 1:1，横竖屏都不拉伸。新关卡沿用这套布局。
- **拖放交换**：松手时若目标格已被占，则**交换**——被占的碎片回到本次拖拽的「来处」（来自某格→对调格子；来自外围→被挤回外围原点）。空格则直接放入。
- **视频关（`drawVideoSrc(octx, info, src)`，可复用）**：每个 `src` 懒建一个隐藏 `<video>`（静音/循环/`playsInline`）；**进游戏即 `preloadVideos()` 预加载全部**（`VIDEO_SRCS` 列表）；画当前关时**暂停其它所有视频**，只留这一路在播，避免多路解码卡顿；每帧 cover 裁剪画进离屏；胜负暂停定格。注意：长视频在不支持 Range 的服务器（如 Python `http.server`）上会卡，线上托管/Pages 支持 Range 则正常。
- **摄像头关（`drawCamera` + `camEnsure`/`camStop`）**：`getUserMedia` 取流、镜像呈现；**仅在该关捕捉**——`loadLevel` 据 `usesCamera` 进则申请、离则 `camStop()` 停掉轨道（相机灯熄灭）。
- **调试作弊块**：`index.html` 末尾有一段「★ 调试作弊（提交前删整段）」——键盘 1–5 跳关、W 一键通关，触屏左上角小按钮同效。**提交前务必删除整段。**

---

## 七、代码结构约定

按这几块组织，清晰、加注释，方便我跟得上：

1. **关卡数据**（`LEVELS` 数组 + 当前关卡指针）—— 多关后新增的一块，放最前面。
2. **画布与画笔**（主 canvas + 离屏 canvas）。
3. **游戏状态**（集中放：当前关卡、startTime、各碎片当前位置/目标格子/是否就位、网格占用、胜负等）。
4. **输入**（拖拽：pointer 事件）。
5. **游戏循环**（requestAnimationFrame：每帧 update（推进倒计时、判胜负）→ draw（调当前关 `renderFrame` 重画离屏、画所有碎片））。
6. **关卡流程**（进入关卡 / 下一关 / 调用钩子 的胶水逻辑）。
7. **启动**。

此外，各关的**渲染器**（`drawSevenSeg` / `drawSnake` / `drawVideoSrc` / `drawCamera` 及各自辅助）单独成块，放在游戏循环之后、浮层之前；引擎只通过 `renderFrame` 调它们。

> 引擎代码与单个关卡的内容尽量分开：通用流程别写死某一关的逻辑，关卡特有的东西放进各自的配置/钩子/渲染器里。

---

## 八、给你的工作方式（agent 行为）

- **小步快跑**：一次只改一件事，改完说清改了什么、我该怎么验证。
- 较大改动先用 **plan 模式**给我看方案，确认再动。
- **每到一个能跑的稳定版本，提醒我**（我会用 checkpoint 留档/回退）。
- **保持简单**，能简单实现就别过度设计。不擅自加依赖、扩 scope、加钩子，拿不准就问。
- 代码可读、有注释——我要看得懂自己的工程。

---

## 九、优先级（时间压力下的取舍）

1. 永远有一个**能跑、能玩完一局**的版本最重要。
2. 先做到「能拖、能拼对、倒计时会动、到点判胜负、能进下一关」，再谈美化/新机制/干扰。
3. 加新关卡时，**先复用现有引擎**，确认能跑，再往里塞这关特有的机制。
4. 临近结束**停止加功能**，只修 bug、调平衡、确保能提交。

---

## 十、提交收尾清单（jam 最后 1~2 小时照这个走）

> 本次 jam 提交要求：**itch.io 可玩链接 + GitHub 源码链接，两个都要。**

- [ ] **删除 `index.html` 末尾的「★ 调试作弊」整段**（跳关 / 一键通关 / 左上角调试按钮）。
- [ ] 干净浏览器里**逐关完整玩一遍**（视频/摄像头关要在服务器/线上、且摄像头在 https 下），确认能拖、能拼、能交换、倒计时正常、胜负判定对、关卡依次推进。
- [ ] **itch.io**：`index.html` + **`VideoAsset0.mp4` / `VideoAsset1.mp4`** 一起打包 zip，**index.html 在 zip 根目录**（别套文件夹）；勾「This file will be played in the browser」；设好视口尺寸；亲自打开玩一遍确认视频能加载、摄像头能授权（itch 是 https）。
- [ ] **GitHub**：推送源码（含 mp4）；仓库 **public**；README 写清玩法、操作、**素材出处/授权**。
- [ ] jam 提交页同时填 **itch 链接 + GitHub 链接**。
- [ ] **提前交一版能跑的**，别卡最后一秒。

---

## 十一、已定 / 待确认事项

**已定：**
- 胜负/计时规则：到 00 未拼完判负；归零前拼完胜利。
- 辅助提示：先不加任何提示，做最纯粹的版本。
- 多关架构：**配置对象 + 可选生命周期钩子**；关卡推进用**线性闯关**（暂不做自由选关界面）。
- **已做 5 关**（见一节清单）：倒计时（数码管）/ 贪吃蛇 / 影像一 / 影像二 / 摄像头。统一 4×3=12 块，均走默认胜负判定（未用钩子）。
- 拖放支持**交换位置**（拖到已占格 = 对调）。
- 视频统一静音循环、进游戏预加载、只播当前关；摄像头仅第 5 关捕捉。

**待确认 / 收尾相关：**
- 视频素材 `VideoAsset0/1.mp4` 的**出处与授权**——README 里待补（务必是自有 / CC / CC0，别用受版权保护的下载）。
- 自定义域名当前跳转到 **http**，而摄像头关需 **https**——上线前确认 itch（https）/Pages 强制 https 可用。
- 关卡间是否要有过场、累计成绩、解锁动画等（先不做，除非确认）。
